[tool.poetry]
name = "orderbot-webpage"
version = "0.1.0"
description = ""
authors = ["keepselvesreal <keep_selfs_real@naver.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
